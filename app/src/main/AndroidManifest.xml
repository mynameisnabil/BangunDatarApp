<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test.bangundatarapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Segitiga"
            android:label="@string/title_segitiga">

        </activity>
        <activity android:name=".jajargenjang"
            android:label="@string/title_jajargenjang">

        </activity>
        <activity android:name=".belahketupat"
            android:label="@string/title_belahketupat">
        </activity>

        <activity android:name=".Persegi"
            android:label="@string/title_persegi">
        </activity>
    </application>

</manifest>